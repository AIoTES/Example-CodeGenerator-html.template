#macro(individuals)
    #set($r = $reasoner.getReasoner())

    #foreach($y in $r.getInstances($class))
    $y
    #end
    #foreach($item in $EntitySearcher.getInstances($class, $ontology))
        <tr>
            <td>${item.getIRI().getFragment()}</a></td>
            <td>$esc.getText("${item.getIRI().getFragment()}")</td>
        </tr>
    
    #end

#end

#macro(data_props)

        <tr>
            <td> ${o.getOntologyID().getOntologyIRI().get()}</a></td>
            <td>#foreach($ann in $o.getAnnotations()) #if($ann.getValue()) $ann.getValue() #end #end</a></td>
        </tr>

#end

#macro(data_types)


        <tr>
            <td> ${o.getOntologyID().getOntologyIRI().get()}</a></td>
            <td>#foreach($ann in $o.getAnnotations()) #if($ann.getValue()) $ann.getValue() #end #end</a></td>
        </tr>


#end


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>CodeGenerator tool Tutorial</title>
    <style>
            table, th, td {
              border: 1px solid black;
              border-collapse: collapse;
            }
            th, td {
              padding: 5px;
              text-align: left;    
            }
    </style>
</head>
<body>
    <h1>${class.getIRI().getFragment()}</h1>
    <h2>In the following list you can see al related elements to this class, like instances, properties,etc.</h2>
   
    <h2>individuals</h2>
        <table>
        <tr>
            <th>individual</th>
            <th>individual IRI</th>
        </tr>
    #individuals()
    </table>
</body>
</html>